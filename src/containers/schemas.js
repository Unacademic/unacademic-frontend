import waypointSchema from "./waypoint.yml";
import checkpointSchema from "./checkpoint.yml";
import resourceSchema from "./resource.yml";
import fallthroughSchema from "./fallthrough.yml";

let schemas = [ waypointSchema, checkpointSchema, resourceSchema, fallthroughSchema ];

export default schemas;
